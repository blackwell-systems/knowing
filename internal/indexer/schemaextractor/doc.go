// Package schemaextractor provides an extractor for OpenAPI 3.x, Swagger 2.x,
// and JSON Schema files. It parses .yaml, .yml, and .json files, producing
// nodes for API endpoints (routes) and schema definitions (types), plus edges
// for $ref relationships between them.
package schemaextractor
