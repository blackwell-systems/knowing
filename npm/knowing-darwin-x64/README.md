# @blackwell-systems/knowing-darwin-x64

Platform-specific binary package for knowing (darwin-x64).

This package is installed automatically as an optional dependency of `@blackwell-systems/knowing`. You do not need to install it directly.

See https://github.com/blackwell-systems/knowing
