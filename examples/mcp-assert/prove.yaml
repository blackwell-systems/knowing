name: prove generates inclusion proof
server:
  command: ./knowing
  args: [mcp, -db, knowing.db]
assert:
  tool: prove
  args:
    source: "NewSQLiteStore"
    target: "Migrate"
    edge_type: "calls"
  expect:
    not_error: true
    contains: ["verified", "true", "proof_steps"]
timeout: 30s
