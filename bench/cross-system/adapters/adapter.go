// Package adapters provides system-specific implementations of the benchmark Adapter interface.
package adapters

// Re-export the interface from benchtype so consumers import one package.
// Implementations are in separate files within this package.
