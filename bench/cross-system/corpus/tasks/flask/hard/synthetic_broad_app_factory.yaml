id: flask-hard-200
repo: flask
tier: hard
description: "Trace the full application factory pattern: from create_app through blueprint registration, extension initialization, config loading, and how the app becomes ready to serve requests"
source: synthetic
ground_truth:
  - Flask.__init__
  - App.register_blueprint
  - Blueprint.register
  - Config.from_object
  - Flask.ensure_sync
  - Flask.run
tags: [broad-scope, architecture, app-factory, initialization]
notes: "Tests understanding of Flask's initialization sequence. Requires linking the app constructor, configuration system, blueprint registration machinery, and the WSGI server startup path."
