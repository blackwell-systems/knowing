id: fastapi-medium-004
repo: fastapi
tier: medium
description: "Add a WebSocket endpoint with dependency injection that authenticates the connection and handles bidirectional messaging"
source: manual
ground_truth:
  - APIWebSocketRoute
  - APIWebSocketRoute.__init__
  - get_websocket_app
  - websocket_session
  - APIRouter.websocket
  - solve_dependencies
tags: [websocket, dependency-injection, cross-module, feature-addition]
notes: "Developer needs APIWebSocketRoute for the route type, get_websocket_app which builds the ASGI handler, websocket_session which wraps the endpoint execution, the websocket decorator for registration, and solve_dependencies for auth injection."
