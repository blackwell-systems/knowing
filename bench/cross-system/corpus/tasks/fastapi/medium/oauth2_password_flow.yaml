id: fastapi-medium-002
repo: fastapi
tier: medium
description: "Implement OAuth2 password bearer authentication with scopes for an API that protects different endpoints with different permission levels"
source: manual
ground_truth:
  - fastapi.security.oauth2.OAuth2PasswordBearer
  - fastapi.security.oauth2.OAuth2PasswordBearer.__call__
  - fastapi.security.oauth2.OAuth2PasswordRequestForm
  - fastapi.security.oauth2.SecurityScopes
  - fastapi.security.oauth2.SecurityScopes.__init__
  - fastapi.param_functions.Security
tags: [security, oauth2, cross-module, feature-addition]
notes: "Developer needs OAuth2PasswordBearer for the token URL scheme, its __call__ for request extraction, OAuth2PasswordRequestForm for the login endpoint, SecurityScopes for scope-based authorization, and the Security function to declare scoped dependencies."
