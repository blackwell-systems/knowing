id: kafka-hard-004
repo: kafka
tier: hard
description: "Debug network timeout issues by tracing through the NetworkClient, Selector, and connection state machine that manages broker connections"
source: manual
ground_truth:
  - NetworkClient
  - NetworkClient.poll
  - NetworkClient.ready
  - Selector
  - KafkaChannel
  - ClusterConnectionStates
tags: [networking, connections, timeout, cross-package, debugging]
notes: "Network debugging requires NetworkClient (protocol layer), its poll loop, ready check for connection state, Selector for NIO, KafkaChannel for per-broker streams, and ClusterConnectionStates for tracking."
