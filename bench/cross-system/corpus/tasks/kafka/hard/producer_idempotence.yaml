id: kafka-hard-002
repo: kafka
tier: hard
description: "Debug why messages are being duplicated by tracing through the idempotent producer's sequence number tracking, transaction coordinator, and epoch fencing"
source: manual
ground_truth:
  - Sender
  - Sender.sendProducerData
  - TransactionManager
  - TransactionManager.maybeResolveSequences
  - ProducerBatch
  - RecordAccumulator
tags: [producer, idempotence, transactions, cross-package, debugging]
notes: "Idempotent producer debugging requires the Sender thread, TransactionManager for sequence tracking, ProducerBatch for retry handling, and RecordAccumulator for batching."
