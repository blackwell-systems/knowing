id: ripgrep-medium-003
repo: ripgrep
tier: medium
description: "Add support for searching only within a specific byte range of each file, useful for searching large log files"
source: manual
ground_truth:
  - Searcher
  - SearcherBuilder
  - Searcher.search_path
  - Searcher.search_reader
  - Sink
tags: [searcher, config, feature-addition]
notes: "The searcher crate provides Searcher (configured via SearcherBuilder) which drives search over files (search_path) or readers (search_reader). Results are delivered to a Sink implementation."
