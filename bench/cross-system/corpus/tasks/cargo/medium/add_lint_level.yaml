id: cargo-medium-002
repo: cargo
tier: medium
description: "Add a new cargo lint that warns when a workspace member has dependencies not shared by any other workspace member, indicating possible extraction"
source: manual
ground_truth:
  - Workspace
  - Workspace.members
  - analyze_cargo_lints
  - Package
  - Package.dependencies
  - Dependency
  - DiagnosticPrinter
tags: [lints, workspace, cross-package, feature-addition]
notes: "Adding a cargo lint requires the workspace API (Workspace, members), the lint analysis infrastructure (analyze_cargo_lints), package and dependency access (Package.dependencies, Dependency type), and diagnostic reporting (DiagnosticPrinter)."
