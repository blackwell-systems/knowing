id: cargo-medium-007
repo: cargo
tier: medium
description: "Generate a build plan that shows compilation order and parallelism for all crates in the dependency graph"
source: manual
ground_truth:
  - JobQueue
  - JobQueue<'gctx>.execute
  - JobQueue.enqueue
  - BuildContext
  - resolve_std
tags: [compilation, parallelism, dependency-graph, cross-package]
notes: "Build plan requires JobQueue for scheduling and execution, BuildContext for configuration, and resolve_std for standard library handling."
