id: cargo-hard-001
repo: cargo
tier: hard
description: "Debug and fix a dependency resolution failure where two workspace members require incompatible versions of the same crate due to a yanked intermediate version"
source: manual
ground_truth:
  - resolve
  - ResolverContext
  - activation_error
  - RegistryQueryer
  - ConflictReason
  - RegistryIndex
  - PackageRegistry
  - Workspace
tags: [resolver, registry, cross-package, bug-fix, cross-cutting]
notes: "Debugging resolution failures requires understanding the resolver loop (resolve), its state tracking (ResolverContext), error generation (activation_error), how the registry is queried (RegistryQueryer, RegistryIndex), conflict classification (ConflictReason), and workspace handling."
