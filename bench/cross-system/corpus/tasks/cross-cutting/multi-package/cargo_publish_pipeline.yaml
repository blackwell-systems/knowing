id: cross-cutting-multi-004
repo: cargo
tier: hard
description: "Extend cargo publish to support pre-publish hooks that run clippy, verify the package, check documentation coverage, and gate on all checks passing before uploading to the registry"
source: synthetic
ground_truth:
  - src/cargo/ops/cargo_package.rs.package
  - src/cargo/ops/cargo_package.rs.PackageOpts
  - src/cargo/ops/registry/publish.rs.publish
  - src/cargo/ops/registry/publish.rs.PublishOpts
  - src/cargo/sources/registry/mod.rs.RegistryData
  - src/cargo/core/compiler/mod.rs.compile
  - src/cargo/util/config/mod.rs.GlobalContext
  - src/cargo/ops/cargo_doc.rs.doc
tags: [publish, registry, compiler, cross-package, cross-cutting, feature-addition]
notes: "Pre-publish hooks span the package/publish pipeline (package, publish with their opts), registry interaction (RegistryData), compilation for running clippy (compile), config for hook definitions (GlobalContext), and documentation (doc). Tests multi-subsystem orchestration."
