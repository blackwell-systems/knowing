id: typescript-hard-201
repo: typescript
tier: hard
description: "Understand how a source file goes from parsed AST through transformation, emit, and source map generation, including how original positions are tracked across transforms"
source: synthetic
ground_truth:
  - typescript.emitter.emitFiles
  - typescript.emitter.emitSourceFileOrBundle
  - typescript.transformer.transformNodes
  - typescript.sourcemap.createSourceMapGenerator
  - typescript.sourcemap.SourceMapGenerator.addMapping
  - typescript.factory.setOriginalNode
  - typescript.emitter.pipelineEmitWithSourceMaps
tags: [cross-package, emit, sourcemap, transform, multi-system]
notes: "Tests cross-package flow. Emit involves the transformer pipeline, the emitter itself, source map generator, and the factory's original node tracking working in concert."
