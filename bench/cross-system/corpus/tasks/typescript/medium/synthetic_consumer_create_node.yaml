id: typescript-medium-201
repo: typescript
tier: medium
description: "Find all transformers that call factory.createIdentifier to understand what would need updating if we added a new parameter to track identifier origin"
source: synthetic
ground_truth:
  - src/compiler/factory/nodeFactory.ts.getDefaultTagNameForKind
  - src/compiler/factory/nodeFactory.ts.propagateChildFlags
  - src/compiler/transformer.ts.getScriptTransformers
  - src/compiler/transformers/classFields.ts.ClassLexicalEnvironment
  - src/services/services.ts.createNode
tags: [api-consumer, factory, transformers, impact-analysis]
notes: "Tests finding consumers of a factory API. createIdentifier is called from dozens of transformer modules; changing its signature requires finding every call site across all transformation passes."
