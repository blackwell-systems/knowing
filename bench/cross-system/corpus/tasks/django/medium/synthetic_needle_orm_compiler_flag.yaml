id: django-medium-200
repo: django
tier: medium
description: "Find the exact place where the SQL compiler decides whether to add DISTINCT to a COUNT aggregate when the queryset uses a multi-table inheritance join"
source: synthetic
ground_truth:
  - SQLCompiler.get_distinct
  - Count.as_sql
  - SQLCompiler.as_sql
tags: [needle-in-haystack, orm, compiler, single-symbol]
notes: "Tests ability to find a very specific decision point in the SQL compilation pipeline. The codebase has hundreds of compiler methods; only one handles the DISTINCT-in-COUNT logic."
