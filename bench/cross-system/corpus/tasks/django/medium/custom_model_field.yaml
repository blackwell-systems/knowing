id: django-medium-003
repo: django
tier: medium
description: "Implement a custom encrypted model field that transparently encrypts data before storage and decrypts on retrieval, with migration support"
source: manual
ground_truth:
  - Field
  - Field.from_db_value
  - Field.get_prep_value
  - Field.deconstruct
  - CharField
  - ModelState.from_model
  - BaseDatabaseSchemaEditor.column_sql
tags: [models, fields, migrations, cross-package, feature-addition]
notes: "Custom fields require understanding the Field class lifecycle methods (from_db_value for read, get_prep_value for write, deconstruct for migrations), a concrete field as a pattern (CharField), migration state handling (ModelState.from_model), and schema generation (column_sql)."
