id: kubernetes-hard-200
repo: kubernetes
tier: hard
description: "Explain the full pod scheduling pipeline from when a pod enters the scheduling queue to when it's bound to a node, including all extension points"
source: synthetic
ground_truth:
  - Scheduler.Run
  - Scheduler.ScheduleOne
  - Scheduler.schedulingCycle
  - Scheduler.bindingCycle
  - Scheduler.assume
  - PriorityQueue
  - Scheduler.findNodesThatFitPod
tags: [broad-scope, architecture, scheduler-pipeline, cross-package]
notes: "Tests architectural understanding retrieval. The scheduling pipeline spans queue management, framework extension points, and binding logic across multiple packages."
