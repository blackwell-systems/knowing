id: kubernetes-medium-200
repo: kubernetes
tier: medium
description: "Find the function that calculates the jitter duration for leader election lease renewal retry in the controller manager"
source: synthetic
ground_truth:
  - NewLeaderElector
  - RunOrDie
  - Jitter
tags: [needle-in-haystack, leader-election, jitter, single-symbol]
notes: "Tests precision in a massive codebase. Jitter is used in dozens of places across kubernetes; the specific lease renewal jitter in leader election is one exact call site."
